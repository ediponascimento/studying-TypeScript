const today = new Date();
console.log(today.getDate());

const person = {
  age: 20
};

class Color {

}

const color = new Color();
console.log(color.constructor.name); // return the class name


console.log(person);
console.log(person.age);